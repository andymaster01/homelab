# ============================================================
# Photos 
# ============================================================

# Photos → OneDrive (rclone) — disaster recovery + plain file sync
[photos-onedrive]
inherit = "default"
repository = "rclone:onedrive:restic_backups/photos"

[photos-onedrive.env]
RESTIC_PASSWORD = "{{ .Env.RESTIC_PHOTOS_PASSPHRASE }}"

[photos-onedrive.backup]
source = ["/mnt/source/pv/photos"]
exclude = ["*.DS_Store", "*/.Trash*"]
schedule = "02:30"
schedule-permission = "system"
tag = ["photos"]
run-after = ["rclone sync /mnt/source/pv/photos onedrive:medias/photos --transfers=4 --checkers=8"]

[[photos-onedrive.backup.send-before]]
url = "{{ .Env.HEALTHCHECKS_PHOTOS_ONEDRIVE_URL }}/start"
method = "HEAD"

[[photos-onedrive.backup.send-after]]
url = "{{ .Env.HEALTHCHECKS_PHOTOS_ONEDRIVE_URL }}"
method = "HEAD"

[[photos-onedrive.backup.send-after-fail]]
url = "{{ .Env.HEALTHCHECKS_PHOTOS_ONEDRIVE_URL }}/fail"
method = "HEAD"

[photos-onedrive.forget]
keep-daily = 7
keep-weekly = 4
keep-monthly = 12
keep-yearly = 3
prune = true
schedule = "02:45"
schedule-permission = "system"

[photos-onedrive.check]
read-data-subset = "5%"
schedule = "Sun 05:00"
schedule-permission = "system"
